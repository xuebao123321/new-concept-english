export default function LoginPage() {
  console.log('LOGIN PAGE RENDERED');
  return (
    <div style={{
      background: 'red',
      color: 'white',
      padding: 60,
      fontSize: 30,
      fontWeight: 'bold',
      textAlign: 'center',
      minHeight: '100vh',
    }}>
      🔴 RED SCREEN — LOGIN PAGE RENDERED
    </div>
  );
}
